const db = require("../config/configDb");
const mongoose = require("mongoose")


let DbMigration = new mongoose.Schema({
    //dados da migração
//esta na pasta de migração  do postman  
})

module.exports = DbMigration